---
id: F001
dependencies: []
scope:
  - src/greet.js
  - src/greet.test.js
validation:
  - test
---

Add `src/greet.js` exporting `greet(name)`. `greet("Ada")` must return the string `hello, Ada`. Empty or non-string input must throw a `TypeError`. Add tests in `src/greet.test.js` run by `npm test`. Done when all greet tests pass and `npm test` is green. Do not change `src/sum.js`, `README.md`, or any other files.
