---
id: F002
dependencies: []
scope:
  - src/sum.js
  - src/sum.test.js
validation:
  - test
---

Add `src/sum.js` exporting `sum(a, b)`. Both arguments must be finite numbers; return their sum. Otherwise throw a `TypeError`. Add tests in `src/sum.test.js` run by `npm test`. Done when all sum tests pass and `npm test` is green. Do not change `src/greet.js`, `README.md`, or any other files.
