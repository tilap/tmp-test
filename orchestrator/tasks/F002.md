---
id: F002
dependencies: []
scope:
  - src/sum.js
  - src/sum.test.js
validation:
  - test
---

Add src/sum.js exporting sum(a, b). Both arguments must be finite numbers; return their sum. Non-finite or non-number arguments must throw TypeError. Add src/sum.test.js with coverage for valid sums and invalid inputs; npm test must pass. Do not change src/greet.js, README.md, or any other existing src files.
