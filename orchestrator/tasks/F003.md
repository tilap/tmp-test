---
id: F003
dependencies:
  - F001
  - F002
scope:
  - README.md
validation:
  - test
---

Rewrite the **Usage** section of `README.md` to show how to import and call `greet` and `sum`. Done when Usage documents both functions with import and call examples and `npm test` still passes. Do not change any file under `src/`.
